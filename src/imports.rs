use crate::types::*;
use derive_header::MutSlice;
use std::io::prelude::*;
use std::io::Read;
/*
#[derive(SimpleValNew)]
pub struct ImportDescriptor {
    pub original_first_thunk: SimpleVal<u32>,
    pub time_data_stamp: SimpleVal<u32>,
    pub forwarder_chain: SimpleVal<u32>,
    pub first_thunk: SimpleVal<u32>,
}
*/
